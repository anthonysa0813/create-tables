# Notas:

```
  Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     the base required by the table [number] [required] [default: 5]
  -l, --listar   drawing the list in the terminal     [boolean] [default: false]
  -h, --hasta    the table required this number by execute the program by
                 default is 10                            [number] [default: 10]
```
